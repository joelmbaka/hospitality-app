// Fallback stub to satisfy TypeScript resolution.
// At runtime Metro will prefer platform-specific implementations
// in cartStorage.web.ts or cartStorage.native.ts.
const storage: any = undefined;
export default storage;
